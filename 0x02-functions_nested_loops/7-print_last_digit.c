#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @a: input number
 * Return: last digit
 */

int print_last_digit(int a)
{
	int dig = a % 10;

	_putchar(dig);
	_putchar(dig);

	return (0);
}
