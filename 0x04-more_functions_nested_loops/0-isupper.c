#include "main.h"
#include <stdio.h>

/**
 *  _isupper - checks if a charecter is uppercase
 *  @c: input charecter
 *
 * Return: 1 if is upper else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1)
	}
	else
	{
		return (0);
	}
}
